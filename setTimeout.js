// Define a function to be executed after a delay
function greet() {
  console.log("Hello, world!");
}

// Call setTimeout() to schedule the function execution after 2 seconds
setTimeout(greet, 2000);

// Output: Hello, world! (after 2 seconds)

// Define a function to animate an element
function animateElement() {
  // Code to animate the element goes here
}

// Call setTimeout() to schedule the animation after 1 second
setTimeout(animateElement, 1000);

// Output: Animation starts after 1 second

// Define a function to animate 4 images
function animateImages() {
  // Code to animate the images goes here
}

// Call setTimeout() to schedule the image animation after 1 second
setTimeout(animateImages, 1000);

// Output: Image animation starts after 1 second